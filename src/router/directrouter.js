const getGitWare = require('../directuse/iweb/getgitware')
direct = () => {
    // let timeer = 100 * 60 * 60
    // setTimeout(() => {
    getGitWare.getware()
        // }, timeer)
}
module.exports = {
    direct
}